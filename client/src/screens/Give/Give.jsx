import React, { useLayoutEffect } from 'react'

function Give() {

    useLayoutEffect(() => {
        window.scrollTo(0, 0)
    });

    return (
        <div className=" w-full h-screen bg-[white] mt-[100vh]">
            
        </div>
    )
}

export default Give 
